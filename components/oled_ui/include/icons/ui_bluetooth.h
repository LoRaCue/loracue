#pragma once

#include <stdbool.h>

/**
 * @brief Draw Bluetooth icon
 * @param enabled Whether Bluetooth is enabled
 * @param connected Whether Bluetooth is connected
 */
void ui_bluetooth_draw(bool enabled, bool connected);
