#pragma once

#include "ui_config.h"

/**
 * @brief Draw presenter main screen (PRESENTER with PREV/NEXT)
 * @param status Current UI status data
 */
void presenter_main_screen_draw(const ui_status_t* status);
