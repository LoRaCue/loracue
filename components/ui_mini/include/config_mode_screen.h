#pragma once

/**
 * @brief Draw configuration mode screen (WiFi AP credentials)
 */
void config_mode_screen_draw(void);

/**
 * @brief Toggle between QR code and text display
 */
void config_mode_screen_toggle_display(void);

/**
 * @brief Reset configuration mode screen state
 */
void config_mode_screen_reset(void);
